version: '3'
services:
  db:
    container_name: leafnode_db
    restart: always
    image: postgres
    ports:
      - 5432:5432
    volumes:
      - ~/apps/postgres:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_DB=leafnode_db
  api:
    container_name: leafnode_api
    restart: always
    image: toreanjoel/leafnode:latest
    build: 
      context: ./
      dockerfile: ./Dockerfile
    environment:
      - PORT=5000
      - DB_HOSTNAME="db"
      - DB_NAME=leafnode_db
      - DB_USER=postgres
      - DB_PASS=postgres
      - DB_PORT=5432
      - APP_DATA=DATA
    ports:
      - "3000:5000"
    depends_on:
      - db
