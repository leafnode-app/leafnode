defmodule LeafNode.Servers.TriggerProcessing do
  @moduledoc """
    Used to hold all of the functions that will be used for AI processing of input data
  """

  @doc """
    Trigger the processing that will be done against AI with relevant input data
  """
  def processing_input(payload, node) do
    # This needs to be done
    {:ok, nil}
  end
end
